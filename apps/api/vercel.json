{
  "version": 2,
  "buildCommand": "cd ../.. && pnpm install --no-frozen-lockfile && cd apps/api && pnpm prisma generate && pnpm build",
  "installCommand": "cd ../.. && pnpm install --no-frozen-lockfile",
  "outputDirectory": "dist",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "memory": 1024
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}

