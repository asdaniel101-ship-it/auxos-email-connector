{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter web run build",
  "installCommand": "cd ../.. && CI=true pnpm install --filter web --ignore-scripts --no-frozen-lockfile",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
