{
  "version": 2,
  "buildCommand": "corepack enable && corepack prepare pnpm@10.18.2 --activate && pnpm install --no-frozen-lockfile && cd apps/api && pnpm prisma generate && pnpm build",
  "installCommand": "corepack enable && corepack prepare pnpm@10.18.2 --activate && pnpm install --no-frozen-lockfile",
  "outputDirectory": "apps/api/dist",
  "framework": null,
  "functions": {
    "api/index.ts": {
      "maxDuration": 60,
      "memory": 1024,
      "includeFiles": "apps/api/dist/**"
    }
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ]
}
